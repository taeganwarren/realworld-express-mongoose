import {
    expect 
} from 'chai';
import app from '../api/server.js';
import test_data from './test_data.js';

describe('DELETE /articles/:slug/comments/:id', function() {

    it('should not run tests', function(done) {
        done();
    });
});